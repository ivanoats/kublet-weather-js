{
  "name": "kublet-weather-js",
  "version": "0.0.1",
  "description": "displays weather data on kublet via GET \"Server\"",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "echo \"Error: no build specified\" && exit 1",  
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ivan Storck",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^3.3.2",
    "ramda": "^0.30.1"
  }
}
